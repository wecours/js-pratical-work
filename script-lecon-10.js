// Instruction et Expression
// Instruction
if(x > 0){
    // Instruction
}
let n = 8;

// Operateur Ternaire
// Outils de condition expression

// Example : dont usse Ternary
let resultat = null;
if(n > 4){
    resultat = true
}else{
    resultat = false
}

// Equivalent of this example in ternary mode
let resultat2 = n > 4 ? true : false